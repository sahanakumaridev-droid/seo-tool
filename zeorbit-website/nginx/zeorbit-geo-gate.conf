# Do not rewrite indexable URLs to /us-only.html.
# Serving us-only on the same path as 200 + noindex hides the site from
# ChatGPT, Safari, and other fetches that use a normal browser UA.
# Keep /us-only.html as its own noindex URL if you still need a US-only notice.
