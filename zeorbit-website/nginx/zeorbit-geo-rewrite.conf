# Must be included at the top of the server{} (before locations).
# Discovery: never rewrite the request URI to us-only.html.
set $zeorbit_ok $zeorbit_access;
if ($remote_addr ~ "^(127\.0\.0\.1|::1)$") { set $zeorbit_ok 1; }
